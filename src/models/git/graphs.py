from models.git.delay import function as d
from models.git.thru import function as t
from models.git.wndsize import function as w

class PyCache:
    def plotGraphs(noo):
        noof = noo
        d(noof)
        t(noof)
        w(noof)

